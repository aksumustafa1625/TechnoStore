# JIRA integration properties — TEMPLATE (commit-safe)
#
# Copy this file to jira-properties.yaml (which is gitignored), then fill in
# the actual values. The yaml file is loaded by the Mule app via configuration-properties.

jira:
  url: "https://YOUR-WORKSPACE.atlassian.net"
  email: "your-email@example.com"
  api:
    token: "REPLACE_WITH_YOUR_API_TOKEN"
  project:
    key: "TS"
